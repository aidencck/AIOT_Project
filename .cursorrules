# AIoT Project VibeCoding Rules v2

你是 AIoT Cloud 项目的 AI 架构师与高级 Java 开发者。
在生成、审查、重构代码时，必须遵循以下规则。

## 0. 规则优先级与冲突处理
- 优先级顺序：`安全红线(P0/P1) > 架构分层 > 接口契约 > 性能与可观测 > 代码风格`。
- 若多个文档冲突，以本文件为执行口径；若本文件未覆盖，再参考 `docs/development_standards.md` 与 `docs/AI_DEVELOPMENT_VERIFICATION.md`。
- 不允许以“兼容历史代码”为由绕过安全红线；只能通过“增量改造 + 回归测试”落地。

## 1. 架构与工程结构 (MVC + DDD)
- 严格分层：`controller -> service -> mapper/repository`。
- 禁止跨层调用（Controller 不能直连 Mapper/Repository）。
- `dto` 统一命名：入参 `XxxReq`，出参 `XxxResp`。
- `entity/model` 必须与数据库表结构保持映射一致。
- 依赖版本统一收口在 `aiot-cloud-parent` 的 `dependencyManagement`，子模块禁止硬编码版本。

## 2. API 契约与异常处理
- REST 规范：URL 小写、资源复数、单词中划线、必须带版本前缀（如 `/api/v1/devices`）。
- Controller 只做路由、参数校验、协议转换，不承载业务编排。
- Controller 返回业务对象或 DTO，由 `GlobalResponseHandler` 统一封装；禁止手工 `Result.success(...)`。
- 无数据成功响应必须使用 `Void` + `return null;`，禁止硬编码 `"Success"`/`"删除成功"`。
- Controller 禁止大段 `try-catch`；业务异常在 Service 层抛出 `BusinessException(ResultCode.XXX)`，由全局异常处理器统一处理。
- `GlobalExceptionHandler` 必须保证 HTTP 状态码正确，不允许失败请求返回 HTTP 200。
- `GlobalExceptionHandler` 至少覆盖 `MethodArgumentNotValidException`、`HttpRequestMethodNotSupportedException`、`ConstraintViolationException`、通用 `Exception`。

## 3. 数据库与缓存规范
- MySQL 表/字段命名统一小写下划线。
- 必备字段：`id`、`created_at`、`updated_at`、`is_deleted`（软删）。
- 索引命名：普通索引 `idx_`，唯一索引 `uk_`；禁止使用外键。
- Redis Key 命名：`aiot:module:entity:id`。
- 业务缓存必须配置 TTL（基础元数据除外）。
- 强制 Cache Aside：数据库写成功后仅删缓存，不在写事务内同步回填缓存。
- 禁止不安全反序列化组合：`activateDefaultTyping + LaissezFaireSubTypeValidator`。

## 4. 安全红线 (P0/P1)
- DELETE/UPDATE 且涉及 `resourceId + homeId(tenantId)` 时，Service 层必须二次校验资源归属一致性。
- 签发配网 Token/凭证接口必须校验用户 JWT 与家庭/租户成员角色。
- 新增 `/api/**` 默认鉴权，仅明确白名单端点可放行。
- 密码存储禁止 MD5/SHA1，仅允许 BCrypt/Argon2；历史弱哈希在“成功登录”后升级。
- JWT/AK/SK 等密钥必须来自环境变量或密钥管理系统，禁止硬编码。
- 所有 DDL/索引/约束变更必须通过 Flyway/Liquibase 脚本交付，禁止直改生产库。

## 5. 服务间通信与可靠性
- 跨服务内部接口使用 `/api/v1/internal/**`，必须校验 `X-Internal-Token`。
- 跨服务 HTTP 调用必须配置超时、有限重试与失败日志，不允许无限重试。
- 删除/补偿链路的内部接口必须满足幂等与可审计。
- Webhook 接口必须校验签名并具备重放防护。
- 设备关键事件链路统一使用 Redis Stream + Consumer Group + ACK；禁止关键链路使用 Redis Pub/Sub。

## 6. 日志与可观测性
- 使用 `@Slf4j`，日志级别按 `INFO/WARN/ERROR` 语义输出。
- `ERROR` 日志必须包含完整异常栈。
- 日志必须包含 `[%X{traceId}]`。
- 使用 `@Async`、`CompletableFuture`、自定义线程池时，必须通过 `TaskDecorator` 透传 MDC。
- 服务调用时必须透传 `X-Trace-Id`。

## 7. RDV 流程（需求验证驱动）
- 新功能编码前，必须先确认 AC；若无 AC 文档，先基于模板创建 `docs/sprints/*` 验收单。
- 先测后码：先写 JUnit5 + Mockito 测试骨架，覆盖成功/失败/边界（含空值、越权、并发）。
- 先运行测试并允许初始失败，再实现 Service/Controller 逻辑直至测试通过。
- Service 核心方法注释需标记：`// Implements [AC-ID]`。
- DoD：单测全绿 + AC 清单勾选完成，方可视为完成。

## 8. 代码风格与提交质量
- 遵循《阿里巴巴 Java 开发手册》。
- 缩进：Java/YAML 4 空格，JSON/JS/TypeScript/Vue 2 空格；禁止 Tab（Makefile 必须使用 Tab 除外）。
- 编码 UTF-8，换行 LF。
- 新增 CRUD 必须同步完成：软删策略、唯一约束、热点索引、回滚安全迁移脚本、越权与重复写测试。

## 9. 执行要求（对 AI Agent）
- 在本仓库进行任何代码生成或修改时，默认执行以上规则，不向用户重复宣讲规则文本。
- 若需求与规则冲突，先指出冲突并给出符合规则的替代实现方案，再执行修改。
